## Updating the Docker Image for CI

When CommonRoad-Reach changes or if we need any new dependencies, we need to update the Docker image that is used for CI.

To update the Docker image, simply run
```bash
bash update_docker_image.sh
```
and enter your LRZ GitLab credentials when prompted.
